{
  "name": "example",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "build": "tpr src -o dest/mylist.js",
    "example": "npm install && npm run build",
    "hoge": "scalajsc -version"
  },
  "dependencies": {
    "tpr": "file:../"
  }
}
